"""Mortal Condensation (凡人凝期) – Foundation Building.

Mathematical constants: π, e, τ, inf, nan, and their convenient aliases.
All constants are computed using `math` and `numpy` (if needed).
Used across physics and math libraries.
"""

import math
import numpy as np

PI = math.pi
E = math.e
TAU = 2 * PI
INF = float('inf')
NAN = float('nan')

pi = PI
e = E
tau = TAU
